name: "Terraform Deployment"

on:
  workflow_call:
    inputs:
      working-directory:
        required: true
        type: string
      container_version:
        required: true
        type: string
      environment:
        required: true
        type: string
    secrets:
      GCP_SA_KEY:
        required: true

jobs:
  terraform:
    name: Terraform Deployment
    uses: ./cd/staging-deploy.yaml
